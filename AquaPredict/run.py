# =====================================================================
# ملف التشغيل المباشر للسيرفر (Run Application)
# الوظيفة: تشغيل سيرفر Uvicorn برمجياً لضمان استقرار مسارات الاستيراد
# =====================================================================

import uvicorn

if __name__ == "__main__":
    # تشغيل تطبيق FastAPI من داخل ملف api.main وإتاحة إعادة التحميل التلقائي
    uvicorn.run("api.main:app", host="127.0.0.1", port=8000, reload=True)